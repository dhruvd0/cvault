import "package:flutter/material.dart";

class MarginAdmin extends StatelessWidget {
  const MarginAdmin({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        centerTitle: true,
        title: Text("Margin"),
      ),
    );
  }
}
